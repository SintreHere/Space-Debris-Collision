import React from "react";
import ReactDOM from "react-dom/client";
import SakleDashboard from "./components/SakleDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SakleDashboard />
  </React.StrictMode>
);
